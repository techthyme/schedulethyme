# Google Calendar API Configuration (Server-side Service Account)
# Get these from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a new project or select existing
# 2. Enable the Google Calendar API
# 3. Create a Service Account (not OAuth2)
# 4. Download the service account JSON file
# 5. Share your Google Calendar with the service account email

# Service Account Credentials (from the downloaded JSON file)
GOOGLE_PROJECT_ID=your_project_id
GOOGLE_PRIVATE_KEY_ID=your_private_key_id
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
GOOGLE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_CLIENT_ID=your_client_id

# The Google Calendar ID to fetch events from (usually your email for personal calendar)
# You need to share this calendar with the service account email above
GOOGLE_CALENDAR_ID=your-email@gmail.com